# Legacy Archive

This folder stores pre-migration files from the earlier project layout.

They are kept for historical reference but are not part of the active workflow.

Active workflow uses:
- `src/qfm/`
- `configs/`
- `scripts/`
- `tests/`
- `docs/`
