# Assumptions

1. Close-to-close returns are used as tradable return proxy.
2. Rebalance happens at close; next-day returns use new weights.
3. Linear transaction cost approximates total execution frictions.
4. Snapshot data is adjusted/cleaned enough for research demonstration.
5. Factor effectiveness is evaluated cross-sectionally, not per-asset time-series forecasting.
