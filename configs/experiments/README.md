# Experiment Configs

Place experiment-specific YAML files here.

Recommended naming:
- `exp_momentum_shorter_window.yaml`
- `exp_higher_costs.yaml`

Keep each file self-contained so run artifacts can snapshot the exact settings.
